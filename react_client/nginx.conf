user  nginx;
worker_processes  auto;
error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    # 보안 패치: 버전 숨기기
    server_tokens off;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    # =====================================================
    # 1. [함정 카드] IP로 직접 접속하거나, 모르는 도메인은 차단
    # =====================================================
    server {
        listen 80 default_server;
        server_name _;
        
        location = /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
        
        # 444: 응답 없이 연결 끊기 (해커 차단용)
        return 444;
    }

    # =====================================================
    # 2. [진짜 서버] Cloudflare 도메인으로 온 요청만 처리
    # =====================================================
    server {
        listen 80;
        
        # ★ 중요: Cloudflare에 등록한 도메인을 적으세요! 
        server_name nca.paaran.com www.nca.paaran.com;

        # 1. [API 요청 처리]
        location /board {
            proxy_pass http://project-app-lb-113923436-589ab687f83c.kr.lb.naverncp.com;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # 2. [정적 파일 / React 라우팅]
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        # 3. [health check]
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
}